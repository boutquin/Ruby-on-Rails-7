<%#$ This partial renders the header of the page. %>
<header>
    <nav>
        <%# Displays the logo at the top of the page with a link to the home page %>
        <%= link_to image_tag("logo.png"), root_path %>
        <%# Displays the navigation links at the top of the page %>
        <ul>
            <li>
            <%= link_to("All Movies", movies_path) %>
            </li>
        </ul>
    </nav>
</header>